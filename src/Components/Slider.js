import React, { Component } from 'react'
import { Card, CardGroup, Button, image, Container   } from 'react-bootstrap'

export default class Slider extends Component {
    render() {
        return (
            <Container>
            <div >
                <h3 className="slider-style">Ceci Sera le slider d'accueil</h3>
            </div>
            </Container>
        )
    }
}
